---
- name: Deploying Nebula
  hosts: all
  vars_files:
    - ansible.vault
  become_user: "{{ remote_user_name }}"
  roles:
    - { role: "docker", tags: "docker" }
    - { role: "deploy", tags: "deploy" }
